#include <iostream>

using namespace std;

int main() {
    int n;
    if (cin >> n && n > 0) { 
        cout << n << '\n';
        int** matrix = new()
    } else {
        cout << "Ошибка. Проверьте введённые данные\n";
    }
}